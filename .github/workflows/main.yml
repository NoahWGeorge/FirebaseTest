name: Deploy to Firebase Hosting
on:
  push:
    branches:
      - master   # trigger this action when there is a push to the master branch

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v2
        
      - name: Deploy to Firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting
        env:
          FIREBASE_TOKEN: 0dKw1nqhmCcYpCgYIARAAGA0SNgF-L9Ird4_m1aLIY2EXwueudPDHA7wdFPiF5smoUC0LjFzePa32UhnVQbtLcMRSS6oTHjB3yQ
